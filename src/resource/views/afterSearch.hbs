{{!-- <div class="mt-4">
  <div class="row">

    {{#each account}}
      <div class="col-sm-6 col-lg-4">
        <div class="card" style="width: 18rem;">
          
          <div class="card-body">
            <h5 class="card-title">{{this.own}}</h5>
            <p class="card-text">Some quick example text to build on the card
              title and make up the bulk of the card's content.</p>
            <a href="/{{this.accountnumber}}/detail" class="btn btn-primary">Get Details</a>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div> --}}
<style>/* Three columns side by side */
.column {
  float: left;
  width: 33.3%;
  margin-bottom: 16px;
  padding: 0 8px;
}

/* Display the columns below each other instead of side by side on small screens */
@media screen and (max-width: 650px) {
  .column {
    width: 100%;
    display: block;
  }
}

/* Add some shadows to create a card effect */
.card {
  box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
}

/* Some left and right padding inside the container */
.container {
  padding: 0 16px;
}

/* Clear floats */
.container::after, .row::after {
  content: "";
  clear: both;
  display: table;
}

.title {
  color: grey;
}

.button {
  border: none;
  outline: 0;
  display: inline-block;
  padding: 8px;
  color: white;
  background-color: #000;
  text-align: center;
  cursor: pointer;
  width: 100%;
}
            body {
            opacity: 0;
            animation: fadeIn 1s forwards;
            }

            @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
            }
.button:hover {
  background-color: #555;
}</style>
<div class="row">
{{#each account}}

  <div class="column">

    <div class="card">
      <img src="https://thongtinnganhang.com.vn/wp-content/uploads/2020/08/agribank2.png" alt="Jane" style="width:100%">
      <div class="container">
        <h2>{{this.own}}</h2>
         <h3 class="bold">{{this.levels}}</h3>
        <p>Mr.Thuan, besides being the coolest teacher, he also gives hight point to his students</p>

        <a href="/{{this.accountnumber}}/detail"><button class="button btn-primary">Get Details</button></a>
      </div>
    </div>
  </div>
      {{/each}}

</div>
